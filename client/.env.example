# ─── Backend URL ──────────────────────────────────────
# Local dev: leave empty — Vite proxy handles /api → localhost:8000
# Production (Vercel): set this to your Render backend URL (no trailing slash)
VITE_API_URL=https://your-betteresume-api.onrender.com
